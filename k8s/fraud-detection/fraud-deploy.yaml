# https://kubernetes.io/docs/concepts/workloads/controllers/deployment/
apiVersion: apps/v1
kind: Deployment
metadata:
  name: fraud-deployment
  labels:
    app: fraud-deploy
spec:
  selector:
    matchLabels:
      app: fraud-deploy
  replicas: 1
  template:
    metadata:
      labels:
        app: fraud-deploy
    spec:
      # initContainers:
      # Init containers are exactly like regular containers, except:
      # - Init containers always run to completion.
      # - Each init container must complete successfully before the next one starts.
      containers:
        - name: fraud-deploy
          image: v4gish7/fraud-detection-service
          envFrom:
            - secretRef:
                name: myenv
          ports:
            - containerPort: 8083
      restartPolicy: Always
---
